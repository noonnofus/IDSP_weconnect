<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <link rel="stylesheet" href="style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <header><%- include("./homepage_header.ejs") %></header>

    <main class="container py-4">
      <div class="row justify-content-center" id="welcome">
        <div class="col-md-6">
          <form class="input-group mb-3" id="roomForm">
            <input
              type="text"
              class="form-control"
              placeholder="room name"
              id="roomInput"
              required
            />
            <button class="btn btn-primary" type="submit">Enter Room</button>
          </form>
        </div>
      </div>

      <div class="row justify-content-center" id="call">
        <button id="copyButton">Copy room ID</button>
        <h2><%= meetingId %></h2>
        <input type="text" value="<%= meetingId %>" id="textToCopy" class="btn btn-secondary" hidden />
        <div class="col-md-6 text-center mb-3">

          <video
            id="myFace"
            autoplay
            playsinline
            class="w-100"
            style="max-width: 400px; height: 400px"
          ></video>
          <div class="btn-group mt-2">
            <button id="mute" class="btn btn-secondary">Mute</button>
            <button id="camera" class="btn btn-secondary">
              Turn camera off
            </button>
            <select id="cameras" class="form-select"></select>
          </div>
        </div>
        <div class="col-md-6 text-center">
          <video
            id="peerFace"
            autoplay
            playsinline
            class="w-100"
            style="max-width: 400px; height: 400px"
          ></video>
        </div>
      </div>
    </main>
    <footer><%- include("./homepage_footer.ejs") %></footer>
    <script src="/socket.io/socket.io.js"></script>
    <script src="meeting.js"></script>
  </body>
</html>
